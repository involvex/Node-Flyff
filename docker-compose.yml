version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: nodeflyff-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: flyff
      POSTGRES_PASSWORD: flyffpass
      POSTGRES_DB: nodeflyff
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
